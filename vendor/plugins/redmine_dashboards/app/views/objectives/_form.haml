= error_messages_for :objective

.box
  = f.hidden_field :dashboard_id
  %p
    = f.label :name
    %span.required
      *
    = f.text_field :name, :size=>50
  %p
    = f.label :description
    = f.text_field :description, :size=>100

    = f.hidden_field :parent_id
    = f.hidden_field :sequence
    = f.hidden_field :risk_rating
